# Development / testing dependencies.
# Install with:
#   pip install -r requirements-dev.txt
# (or: pip install --trusted-host pypi.org --trusted-host files.pythonhosted.org -r requirements-dev.txt)

-r requirements.txt

pytest>=8.0.0
httpx>=0.27.0   # required by fastapi.testclient
